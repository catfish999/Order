
function changefbnoodle(amount) {
  let input = document.getElementById("fbnoodle");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changefpnoodle(amount) {
  let input = document.getElementById("fpnoodle");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changefnoodle(amount) {
  let input = document.getElementById("fnoodle");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changebcookie(amount) {
  let input = document.getElementById("bcookie");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changepcookie(amount) {
  let input = document.getElementById("pcookie");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changebcake(amount) {
  let input = document.getElementById("bcake");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changepcake(amount) {
  let input = document.getElementById("pcake");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changepcrice(amount) {
  let input = document.getElementById("pcrice");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changebrice(amount) {
  let input = document.getElementById("brice");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changeprice(amount) {
  let input = document.getElementById("price");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
function changeporkchop(amount) {
  let input = document.getElementById("porkchop");
  let origin = parseInt(input.value);
  let newValue = origin + amount;
  if (newValue < 0) newValue = 0;
  input.value = newValue;
}
